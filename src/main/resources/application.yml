spring:
  datasource:
    url: jdbc:mysql://pegfamtree.mysql.database.azure.com:3306/familytreepeg
    username: root1
    password: cometoeat@1

    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect


server:
  port: 8081